import { PROJECT_NAME } from "@ai-office/shared";

export function App() {
  return (
    <main>
      <h1>{PROJECT_NAME}</h1>
      <p>Dashboard foundation package. Visualization work is scheduled for later phases.</p>
    </main>
  );
}
